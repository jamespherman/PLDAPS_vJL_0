function plotSTA_checkerboard(figData, staAccum, staSpikeCount, ...
    selectedChannel) %#ok<INUSD>
% plotSTA_checkerboard  PHASE 3 STUB.
%
%   Phase 3 layout (per analysisPlanningDocs/rfMap_unified_merge_plan.md):
%     - Temporal-kernel traces stacked by (checkSize, contrast)
%     - F1/F2 amplitude bars adjacent
%     - Reporting convention: raw amplitude AND F1/(F1+F2) modulation index
%
%   Phase 1 plumbing exists so the dispatcher can route to it; calling
%   it errors clearly until Phase 3 fills it in.

error('plotSTA_checkerboard:notImplemented', ...
    ['Checkerboard online plot is a Phase-3 deliverable. See ' ...
     'analysisPlanningDocs/rfMap_unified_merge_plan.md  ->  Phase 3.']);

end
